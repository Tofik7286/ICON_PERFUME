import React from 'react'
import Profile from './Profile'

export const metadata = {
  alternates: {
    canonical: `https://www.iconperfumes.in/profile/`,
  }
};

const page = () => {
  return (
    <Profile />
  )
}

export default page
